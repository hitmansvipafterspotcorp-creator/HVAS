{
  "name": "hvas-engine",
  "version": "0.1.0",
  "description": "HITMANS VIP AFTER SPOT — Phaser 4.1.0 + TypeScript game engine",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
